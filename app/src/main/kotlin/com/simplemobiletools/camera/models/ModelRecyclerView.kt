package com.simplemobiletools.camera.models

import android.media.Image
import android.widget.ImageView


class ModelRecyclerView(name:String, image:Int) {

    var mName = name
    var mImage = image
}
