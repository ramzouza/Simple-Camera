language: android

android:
  components:
    - platform-tools
    - tools
    - build-tools-28.0.3
    - android-28
    - extra-android-m2repository
    - sys-img-armeabi-v7a-android-28

jobs:
  include:
    - stage: test
      # Emulator Management: Create, Start and Wait
      before_script:
        - echo "y" | android update sdk -a --no-ui --filter sys-img-armeabi-v7a-android-28,sys-img-x86_64-android-28
        - echo no | android create avd --force -n test -t android-28 --abi armeabi-v7a
        - emulator -avd test -no-skin -no-audio -no-window &
        - android-wait-for-emulator
        - adb shell input keyevent 82 &
      script:
        - ./gradlew test
        - ./ui_test.sh
    - stage: lint
      script: ./gradlew lint
    - stage: build
      script: ./gradlew assembleDebug


notifications:
  slack: mini-capstone-konoha:0QohlfBAaXz34wuCg2SJMQZo
